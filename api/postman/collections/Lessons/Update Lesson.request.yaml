$kind: http-request
name: Update Lesson
url: "{{url}}/courses/{{courseId}}/sections/{{sectionId}}/lessons/{{lessonId}}"
method: PATCH
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
body:
  type: json
  content: |-
    {
      "title": "First Lesson of OOP"
    }
order: 1000000000019
