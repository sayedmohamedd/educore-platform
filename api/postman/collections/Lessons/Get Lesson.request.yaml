$kind: http-request
name: Get Lesson
url: "{{url}}/courses/{{courseId}}/sections/{{sectionId}}/lessons/{{lessonId}}"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 1000000000018
