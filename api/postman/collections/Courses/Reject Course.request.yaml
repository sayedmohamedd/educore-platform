$kind: http-request
name: Reject Course
url: '{{url}}/courses/{{courseId}}/reject'
method: PATCH
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{access_token}}'
body:
  type: json
  content: |-
    {
      "reason": "Does not meet quality standards"
    }
order: 1000000000009
