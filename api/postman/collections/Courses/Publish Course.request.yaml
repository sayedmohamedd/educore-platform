$kind: http-request
name: Publish Course
url: '{{url}}/courses/{{courseId}}/publish'
method: POST
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{access_token}}'
order: 1000000000006
