$kind: http-request
name: Get Enrollment
url: '{{url}}/enrollments/{{enrollmentId}}'
method: GET
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{access_token}}'
order: 2000000000006
